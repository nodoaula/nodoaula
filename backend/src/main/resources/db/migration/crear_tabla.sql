-- Archivo de prueba: el nombre NO sigue V<version>__<descripcion>.sql a propósito.
-- Sirve para comprobar que el check de migraciones bloquea el Pull Request.
-- Esta rama no se fusiona nunca.
SELECT 1;
